# Sub-project sudah masing-masing punya repo Git sendiri (lihat .git di dalamnya),
# jadi tidak perlu (dan sebaiknya tidak) ikut ditrack dari root ini.
sekuritas-api/
sekuritas-frontend/
sekuritas-cms/
sekuritas-ai/
sekuritas-mobile/

# Secrets / env asli — jangan pernah commit, cukup .env.example
.env
env/
envsource/

.DS_Store

# Data sensitif lokal (IP/port SSH, password demo, API key) — JANGAN di-commit (repo publik)
AKUN_DEMO.md
PROMPT_SERVER_LOKAL.md
